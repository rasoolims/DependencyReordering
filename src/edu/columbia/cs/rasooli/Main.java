package edu.columbia.cs.rasooli;

public class Main {

    public static void main(String[] args) {
        /*
        int[] stuff={1,2,3};
        ContextInstance instance=new ContextInstance(1,stuff);
      ArrayList<int[]> permutes= instance.getAllPossibleContexts();
        for(int[] per:permutes)
            System.out.println(Arrays.toString(per));
        */
    }
}
